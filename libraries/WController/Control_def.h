#pragma once
        enum ConnectionState{
        Unauthenticated,Authenticated};
        enum MovementState{
        Forward=1,Reverse=2,Stat=0
        };
        enum TurnState{
        Left=1,Right=2,Idle=0
        };